# PBench Scoring

Read the verifier's structured checks before its aggregate reward. Classify
missing Agent evidence separately from provider, network, browser, or upstream
failure; do not reinterpret synthetic fixtures as Agent results.

The [evaluation reference](../../reference/evaluation.md) owns shared evidence
semantics. The [PBench reference](../../reference/pbench.md) owns Task-specific
scoring and multi-step behavior.
